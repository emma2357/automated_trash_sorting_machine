function d = createDataStore()
    % ------------------------------------------------------------------
    % d - Data Store
    % ------------------------------------------------------------------
    d                  = struct();
    d.nr_objects_found = [];
    d.time             = [];
end